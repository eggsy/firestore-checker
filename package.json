{
  "name": "firestore-checker",
  "version": "1.0.0",
  "description": "Check if song exists in Firestore.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "devScript",
    "build": "rimraf dist && yarn --silent && tsc",
    "start": "cd dist && node index.js"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "consola": "^2.15.0",
    "dotenv": "^8.2.0",
    "firebase": "^8.2.1",
    "get-next-date": "^1.0.0",
    "moment": "^2.29.1",
    "prompts": "^2.4.0",
    "rimraf": "^3.0.2",
    "ts-devscript": "^1.1.1"
  }
}
